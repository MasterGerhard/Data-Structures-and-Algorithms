PuzzleSolve
By Steven Gerhard

compile src and run PuzzleSolve.class

Recursively solves summation puzzles by finding integer values for each unique letter.

pot + pan = bibdog + cat = pigboy + girl = baby
